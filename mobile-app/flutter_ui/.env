# Local testing defaults (edit as needed)
# If using Flutter web, localhost requires special handling; for now use your dev machine LAN IP.
AUTH_BASE_URL=http://localhost:4000
API_BASE_URL=http://localhost:3000

